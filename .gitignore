# Build artifacts
build/
dist/
*.egg-info/
*.egg
__pycache__/
*.py[cod]

# Runtime data (per-persona stores, logs) — never commit
data/
*.jsonl
*.db
*.faiss

# Virtualenvs
.venv/
venv/

# Private extraction tooling — maps real persona names → generic examples, keep local
sync_from_app.sh
